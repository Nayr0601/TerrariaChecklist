node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# editors
.vscode/*
!.vscode/extensions.json
.idea

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# generated test fixtures (binary, regenerate with `npm run generate:fixtures`)
tests/fixtures/*.plr

# item/boss sprites — extracted from Terraria's game files (© Re-Logic);
# kept local-only rather than redistributed from this public repo. See
# README "Attribution" and src/data/sprites.ts / bossSprites.ts for how to
# add your own.
src/assets/sprites/*.png
src/assets/boss-sprites/*.png
